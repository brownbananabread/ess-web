export const metadata = {
  title: 'Contact',
  description:
    'Get in touch with Eternal Safety Solutions. Book a free consultation or request a quote for WHS services.',
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
